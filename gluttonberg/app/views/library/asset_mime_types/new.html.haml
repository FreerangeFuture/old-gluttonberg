%h1 AssetMimeTypes controller, new action

%p 
  Edit this file in 
  %tt app/views/asset_mime_types/new.html.erb

= error_messages_for @asset_mime_type

= form_for(@asset_mime_type, :action => url(:asset_mime_types) ) do
  %p
    = submit "Create"

= link_to 'Back', url(:asset_mime_types)