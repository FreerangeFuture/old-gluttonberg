- @page_title = "Assets - #{@collection.name}"
= sub_nav do
  = back_link("Library", slice_url(:library))
  = nav_link("Delete", slice_url(:delete_collection))
  = nav_link("Edit", slice_url(:edit_collection))
  = nav_link("Add asset", slice_url(:new_asset))

= partial("library/shared/sorter", :opts => {:route => :collection_show, :id => params[:id]})
= partial("library/shared/asset_panels")
