= sub_nav do
  = back_link("Settings", slice_url(:settings))
  = back_link("Dialects", slice_url(:dialects))

= form_for(@dialect, :action => action) do
  = error_messages_for(@dialect)
  = block("Dialect") do
    = text_field(:code, :label => "Code")
    %br
    = text_field(:name, :label => "Name")
    %br
    = check_box(:default)
    %label{:for => "dialect_default", :class => "checkbox"} Make default
  
  = form_controls(slice_url(:dialects))
